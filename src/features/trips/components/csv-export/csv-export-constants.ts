/**
 * CSV Export Constants
 *
 * Category metadata re-exported from the shared column registry.
 * Column definitions live in `export-columns.registry.ts`.
 */

export {
  CATEGORY_ORDER,
  COLUMN_CATEGORIES,
  EXPORT_COLUMN_DEFS as EXPORT_COLUMNS
} from '@/features/trips/lib/export-columns.registry';
