/** Trips included in Schichtzettel reconciliation list (per product scope; see docs). */
export const SHIFT_RECONCILIATION_TRIP_STATUS = 'assigned' as const;

export const SHIFT_RECONCILIATIONS_QUERY_ROOT =
  'shift-reconciliations' as const;

/** German currency formatting for shift tables and summary. */
export const SHIFT_RECONCILIATION_CURRENCY_LOCALE = 'de-DE';
export const SHIFT_RECONCILIATION_CURRENCY_CODE = 'EUR';
